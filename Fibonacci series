#User function Template for python3

class Solution:
    def series(self, n):
        MOD = 10**9 + 7
        fib = [0, 1]
        if n == 0:
            return [0]
        
        for i in range(2, n + 1):
            fib.append((fib[i - 1] + fib[i - 2]) % MOD)
        
        return fib
